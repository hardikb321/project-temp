# Optional: add any other env vars your app needs.
#
# CSV upload: frontend calls /api/upload/presigned (same origin). Your backend
# returns a pre-signed URL; the file is uploaded directly to that URL.
# For local dev, run your backend and set the proxy target in vite.config.ts if needed.
